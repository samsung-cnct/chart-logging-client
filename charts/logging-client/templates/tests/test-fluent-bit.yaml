apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}-fluent-bit-test"
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
  - name: {{ .Release.Name }}-fluent-bit-test
    image: {{ .Values.image }}
    env:
    command: ["sh", "-c", "true"]
  restartPolicy: Never
