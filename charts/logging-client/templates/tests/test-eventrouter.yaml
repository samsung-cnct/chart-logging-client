---
apiVersion: v1
kind: Pod
metadata:
  name: "{{ template "logging-client.name" . }}-test"
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
  - name: "{{ template "logging-client.name" . }}-test"
    image: "{{ .Values.test_image }}"
    command: ["sh", "-c", "true"]
  restartPolicy: Never
