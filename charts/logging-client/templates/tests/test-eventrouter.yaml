apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}-eventrouter-test"
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
  - name: {{ .Release.Name }}-eventrouter-test
    image: {{ .Values.image }}
    env:
    command: ["sh", "-c", "true"]
  restartPolicy: Never
